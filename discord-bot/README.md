# discord-bot

Not built yet. This is build step 5 in `01-station-guess.md`, after the PWA.

When it is: discord.py with slash commands, a `help` command and no `start`,
one process on the VPS, calling the same API as the Telegram bot with
`BOT_API_TOKEN` and `client_key` `dc:<user id>`. SQLite for scheduling and
persistent buttons only; all game state stays behind the API.
